# Intermediate build output
build/
build-debug/
build-*/

# CMake artifacts
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.cmake
.vscode/
.idea/

# opencode local config
.opencode/

# Scratch / repro files
crash_repro.cpp
repr/

# Deployed release runtime (generated by the CMake windeployqt post-build hook)
installer/release/

# Local release packaging output (Setup.exe, zip, deployed binaries)
Release/

# Duplicated release runtime folders (binaries shipped via installer/releases are kept)
# otherwise the repo would balloon. Keep only releases/0.1.6 below.
# (Old versions kept out)
releases/0.1.0/
releases/0.1.5/

# Installer intermediate / logs
*.log
*.zip
*.exe

# Duplicated working copies of the repo (backup clones with their own .git)
CopperBuild/
copperdm/

# Scratch build transcript
build-out.txt
